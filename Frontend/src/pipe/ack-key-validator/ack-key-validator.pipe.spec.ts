import { AckKeyValidatorPipe } from './ack-key-validator.pipe';

describe('AckKeyValidatorPipe', () => {
  it('should be defined', () => {
    expect(new AckKeyValidatorPipe()).toBeDefined();
  });
});
